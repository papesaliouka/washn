import React from 'react';
import {View, StyleSheet} from 'react-native';
import Input from '../../components/input/input.standard.component';

const Wrapper = () => {
    return (
        <View>
            <Input title='ADDRESS' label='(APT/STE)' />
        </View>
    );
}

export default Wrapper;


